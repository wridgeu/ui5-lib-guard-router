{
	"name": "ui5-lib-guard-router-demo",
	"version": "1.0.0",
	"private": true,
	"author": "Marco Beier",
	"repository": {
		"type": "git",
		"url": "https://github.com/wridgeu/ui5-guard-router.git",
		"directory": "packages/demo-app"
	},
	"description": "Demo app for ui5-lib-guard-router library",
	"type": "module",
	"scripts": {
		"clean": "rimraf dist .ui5",
		"serve": "ui5 serve",
		"start": "ui5 serve --open /index.html",
		"build": "ui5 build",
		"test": "echo \"Run tests from monorepo root: npm run test:e2e\" && exit 1",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@ui5/cli": "^4.0.0",
		"ui5-middleware-livereload": "^3.0.0",
		"ui5-tooling-transpile": "^3.0.0"
	},
	"dependencies": {
		"ui5-lib-guard-router": "*"
	}
}
